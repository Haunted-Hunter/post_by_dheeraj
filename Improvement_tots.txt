these are the things that i want to be and improved in this project:

0)The tools should only show up in the chat box interface not outside the application .Those only spawn only when user query about that in the chat interface.(The only main things should be appear to the user is manual data entry ,AI check up ,AI chat agent).
1)remove the architecture graph info when clicking Chat-Based Agent button
2)Tools are for running the commands in terminals when the user got a issue like if the user has suspect that his pc has some issue so that the ai will execute commands in the terminal and find out the problem and give the use a suggestion.
3)same logic interface and chat should also be the telegram backup bot 
but the only big thing that bot can't do is using all tools ,it can just tell about the pc and its issue and ai will suggest what to with it like reuse ,recycle, repair.
4)If any case the reuse comes as a suggestion it can suggest the selling price of other parts and how to make use of other components.
5)The application is so informative in frontend , we should make this application work not a book like reading thing.(remove the content in the front as very short as possible).
6)If the user having issue with the keyword buttons not working then the application should test the user by pressing the button under a certain period of time 

7)The AI should properly take and ingest the output of testing tools (KeyStrike keyboard test and all terminal diagnostic probes) without getting stuck or re-looping into the test itself.
8)Comfortable multi-line chat input: Make the input field comfortable, allowing Shift + Enter to create a new line without sending. Also, when real trouble occurs on the PC, run diagnostic tools and clearly show all three circular options together (Repair, Reuse, Recycle) in the chat widget.
9)Self-improvement & Admin Bot notification: If the user gets an error outside tool handling or an unfamiliar/novel query, notify the admin bot (@AHackBattle013bot) with full context and PC hardware details seeking help, so the self-improving AI can learn permanently from the admin's /reply.
